function Car(parameters) {
    this.transmition = parameters.transmition;
    this.brand = parameters.brand;
    this.model = parameters.model;
    this.price = parameters.price;
    this.engine = parameters.engine;
    this.volume = parameters.volume;
    this.year = parameters.year;
    this.seats = parameters.seats;
    this.doors = parameters.doors;
    this.images = parameters.images;
    this.mainImage = parameters.mainImage;
    this.description = parameters.description;
}